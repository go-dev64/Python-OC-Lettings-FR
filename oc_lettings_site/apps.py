# Django application configuration for the 'oc_lettings_site' app.
# This module defines the configuration for the 'oc_lettings_site' app, including its name.


from django.apps import AppConfig


class OCLettingsSiteConfig(AppConfig):
    """Configuration class for the 'oc_lettings_site' app.

    This class defines the configuration for the 'oc_lettings_site' app, including its name.

    Attributes:
        name (str): The name of the Django app, which is 'oc_lettings_site'.
    """

    name = "oc_lettings_site"
